# Web framework
fastapi

# ASGI server used to run the app
uvicorn[standard]

# HTML templating engine
jinja2

# Database toolkit (ORM)
sqlalchemy==2.0.41

# PostgreSQL driver (works with Supabase)
psycopg[binary]

# Loads variables from the .env file
python-dotenv

# Hashes passwords safely (never store plain-text passwords!)
bcrypt

# Creates and reads JWT access/refresh tokens
PyJWT

# Validates email addresses inside Pydantic schemas (pydantic[email])
email-validator

# Lets FastAPI read form data (used by the interactive /docs login form)
python-multipart

# --- Development / testing only ---
# Test runner for the pytest suite in tests/
pytest
# HTTP client used by FastAPI's TestClient to exercise the API in tests
httpx
